import 'package:flutter/material.dart';

class GeneratePassScreen extends StatelessWidget {
  const GeneratePassScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Generate Pass Screen'),
    );
  }
}